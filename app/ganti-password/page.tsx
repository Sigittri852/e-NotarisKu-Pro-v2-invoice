"use client";import AppShell from '@/components/AppShell';import {useState} from 'react';
export default function Page(){const[msg,setMsg]=useState('');return <AppShell><section className="hero compact"><div><h1>Ganti Password</h1><p>Perbarui password akun administrator secara berkala.</p></div><div className="hero-icon">🔐</div></section><form className="card grid form-grid" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget);const a=String(f.get('baru'));const b=String(f.get('konfirmasi'));setMsg(a.length<8?'Password minimal 8 karakter.':a!==b?'Konfirmasi password tidak sama.':'Password berhasil diperbarui (mode demo).')}}><div className="field full"><label>Password Lama</label><input name="lama" type="password" required/></div><div className="field"><label>Password Baru</label><input name="baru" type="password" required/></div><div className="field"><label>Konfirmasi Password</label><input name="konfirmasi" type="password" required/></div><div className="actions full"><button className="btn btn-primary">Simpan Password</button>{msg&&<span>{msg}</span>}</div></form></AppShell>}
