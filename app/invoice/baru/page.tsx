import AppShell from "@/components/AppShell";import InvoiceForm from "@/components/InvoiceForm";import {getAkta} from "@/lib/store";import {getInvoice} from "@/lib/invoice-store";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{akta?:string;edit?:string}>}){const q=await searchParams;const akta=q.akta?await getAkta(q.akta):undefined;const invoice=q.edit?await getInvoice(q.edit):undefined;return <AppShell><section className="hero compact"><div><h1>{invoice?'Edit Invoice / Tagihan':'Buat Invoice / Tagihan'}</h1><div>{akta?`Dari Akta ${akta.nomorAkta} · ${akta.jenisAkta}`:invoice?`Perbarui ${invoice.nomor}`:'Buat tagihan baru untuk klien.'}</div></div></section><InvoiceForm akta={akta} invoice={invoice}/></AppShell>}
